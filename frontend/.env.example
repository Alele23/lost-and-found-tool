# Copy to .env in this directory. .env is git-ignored.
# Vite only exposes env vars prefixed with VITE_ to the browser.

VITE_API_BASE_URL=http://127.0.0.1:8001

# Must match backend/.env's API_SECRET_KEY.
VITE_API_KEY=
